---
phase: 02-demo-notebook-and-visualizations
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/test_demo_notebook.py
autonomous: true
requirements: [NTBK-01, VIZ-01, VIZ-02, VIZ-03, VIZ-04]

must_haves:
  truths:
    - "Test suite validates notebook structure without requiring full kernel execution"
    - "Tests confirm rcParams are set for projector readability"
    - "Tests confirm all three figure savefig calls are present"
    - "Tests confirm presentation-order cell flow"
    - "All tests pass with python -m pytest tests/test_demo_notebook.py -x"
  artifacts:
    - path: "tests/test_demo_notebook.py"
      provides: "Structural validation tests for the demo notebook"
      min_lines: 60
  key_links:
    - from: "tests/test_demo_notebook.py"
      to: "QVC_QNN_demo.ipynb"
      via: "json.load to parse notebook structure"
      pattern: "QVC_QNN_demo.ipynb"
---

<objective>
Create a pytest test suite that validates the demo notebook's structure, cell ordering, projector-safe settings, and figure generation calls -- all without requiring a running Jupyter kernel or qiskit installation.

Purpose: Automated validation that the notebook meets all Phase 2 requirements (NTBK-01, VIZ-01 through VIZ-04) by inspecting notebook JSON structure. This enables CI-like checks without heavy quantum computing dependencies.

Output: tests/test_demo_notebook.py with 6+ test functions covering notebook existence, cell count, presentation order, rcParams, and figure saves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-demo-notebook-and-visualizations/02-RESEARCH.md
@.planning/phases/02-demo-notebook-and-visualizations/02-01-SUMMARY.md

<interfaces>
<!-- The test file reads the notebook as JSON and inspects cell structure -->

Notebook structure (nbformat 4):
```json
{
  "nbformat": 4,
  "nbformat_minor": 5,
  "metadata": {"kernelspec": {"name": "python3", ...}},
  "cells": [
    {"cell_type": "markdown"|"code", "source": ["line1\n", "line2\n"], "outputs": [], ...},
    ...
  ]
}
```

Existing test patterns from tests/test_cache.py:
- PROJECT_ROOT = Path(__file__).parent.parent
- Uses pytest.mark.skipif for missing files
- Simple _load_json helper
- docstrings reference requirement IDs

Existing conftest.py adds project root to sys.path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_demo_notebook.py with structural validation tests</name>
  <files>tests/test_demo_notebook.py</files>
  <read_first>
    - tests/test_cache.py
    - tests/conftest.py
    - QVC_QNN_demo.ipynb
  </read_first>
  <action>
Create tests/test_demo_notebook.py following the same patterns as test_cache.py (PROJECT_ROOT, skipif decorators, requirement IDs in docstrings). All tests read the notebook as JSON -- NO kernel execution required.

The test file must contain these exact test functions:

**1. test_notebook_exists()**
- Requirement: NTBK-01
- Check that QVC_QNN_demo.ipynb exists at PROJECT_ROOT
- Check it is valid JSON with nbformat == 4
- Check it has a "cells" key that is a non-empty list

**2. test_notebook_cell_count()**
- Requirement: NTBK-01
- Load notebook, count cells
- Assert at least 12 cells total
- Assert at least 5 markdown cells
- Assert at least 7 code cells

**3. test_presentation_order()**
- Requirement: NTBK-01
- Load notebook cells, extract markdown cell content
- Verify the first markdown cell contains "Quantum Variational Circuit" (title)
- Find the markdown cells and verify they appear in this order (by checking substrings):
  1. "Quantum Variational Circuit" (title)
  2. "Input Data" or "Pixel Grid" (data overview)
  3. "Variational Quantum Circuit Approach" or "Circuit" (circuit explanation)
  4. "Training Convergence" or "Loss" (training results)
  5. "Hardware" or "Simulator" (hardware comparison)
  6. "Key Findings" or "Summary" (summary)

**4. test_rcparams_projector_safe()**
- Requirement: VIZ-01
- Find the setup code cell (first code cell)
- Check it contains "rcParams"
- Check it contains "font.size" with a value of 16 or higher (search for "'font.size': 16" or "'font.size': 18" etc.)
- Check it contains "lines.linewidth"
- Check it contains "COLORS" dictionary definition
- Check it contains "os.makedirs" for figures directory

**5. test_figure_saves_present()**
- Requirements: VIZ-02, VIZ-03, VIZ-04
- Concatenate all code cell sources
- Assert "figures/input_data_grid.png" appears (VIZ-04)
- Assert "figures/loss_curve.png" appears (VIZ-02)
- Assert "figures/hardware_comparison.png" appears (VIZ-03)
- Assert the code contains at least 3 occurrences of "savefig"

**6. test_cache_imports_and_loading()**
- Requirements: NTBK-01 (presentation flow depends on cache loading)
- Concatenate all code cell sources
- Assert "from cache_results import" appears (imports shared functions)
- Assert "trained_weights.json" appears (loads cached weights)
- Assert "training_history.json" appears (loads loss history)
- Assert "hardware_results.json" appears (loads hardware results)
- Assert "live_results.json" appears (D-06 auto-detect)

**7. test_live_training_cell()**
- Requirement: NTBK-01 (D-07 live training demo)
- Find a code cell containing "maxiter"
- Assert that cell also contains "minimize" (scipy optimizer)
- Assert that cell contains "3" as the maxiter value (check for "maxiter": 3 or "maxiter: 3" or similar)

**8. test_clean_cell_outputs()**
- Requirement: NTBK-01 (notebook should be committed with clean outputs)
- For every code cell, check that "outputs" is an empty list
- For every code cell, check that "execution_count" is null/None

Implementation notes:
- Use a module-level NOTEBOOK_PATH = PROJECT_ROOT / "QVC_QNN_demo.ipynb"
- Use a notebook_exists skipif decorator like test_cache.py uses for JSON files
- Create a helper _load_notebook() that returns parsed JSON
- All source content must be joined with ''.join(cell['source']) before searching
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_demo_notebook.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_demo_notebook.py exists with 8 test functions
    - grep -c "def test_" tests/test_demo_notebook.py returns 8
    - grep "NTBK-01" tests/test_demo_notebook.py matches (requirement traceability)
    - grep "VIZ-01" tests/test_demo_notebook.py matches
    - grep "VIZ-02" tests/test_demo_notebook.py matches or VIZ references in figure test
    - grep "VIZ-03" tests/test_demo_notebook.py matches or VIZ references in figure test
    - grep "VIZ-04" tests/test_demo_notebook.py matches or VIZ references in figure test
    - python3 -m pytest tests/test_demo_notebook.py -x passes (all tests green)
    - No test requires qiskit, jupyter kernel, or running notebook cells
  </acceptance_criteria>
  <done>
    - tests/test_demo_notebook.py has 8 test functions validating notebook structure
    - All tests pass against the notebook created in Plan 01
    - Tests cover: existence, cell count, presentation order, rcParams, figure saves, cache imports, live training cell, clean outputs
    - Each test docstring references the requirement ID it validates
    - No heavy dependencies required (just json and pathlib)
  </done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_demo_notebook.py -v` -- all 8 tests pass
- `python3 -m pytest tests/ -v` -- full test suite passes (both test_cache.py and test_demo_notebook.py)
</verification>

<success_criteria>
- tests/test_demo_notebook.py exists with 8 test functions
- All 8 tests pass when run against the notebook from Plan 01
- Tests validate notebook structure without requiring Jupyter kernel or qiskit
- Requirement IDs (NTBK-01, VIZ-01-04) referenced in test docstrings
- Full test suite (tests/) passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-demo-notebook-and-visualizations/02-02-SUMMARY.md`
</output>
