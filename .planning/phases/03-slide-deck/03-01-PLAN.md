---
phase: 03-slide-deck
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - slides/generate_deck.py
  - output/presentation.pptx
autonomous: true
requirements: [SLID-01, SLID-02, SLID-03, SLID-04]

must_haves:
  truths:
    - "PowerPoint file opens and contains slides covering training, evaluation, and hardware execution sections"
    - "Transition slides exist between demo segments (training-to-eval, eval-to-hardware) with audience context"
    - "All three notebook-generated figures (input_data_grid.png, loss_curve.png, hardware_comparison.png) are embedded or have placeholder text"
    - "A summary/takeaway slide exists at the end with key results and conclusions"
    - "Every slide has speaker notes guiding the presenter"
  artifacts:
    - path: "slides/generate_deck.py"
      provides: "python-pptx script that builds the complete presentation"
      min_lines: 100
    - path: "output/presentation.pptx"
      provides: "Generated PowerPoint deck ready for presentation"
  key_links:
    - from: "slides/generate_deck.py"
      to: "figures/*.png"
      via: "add_picture with os.path.exists guard"
      pattern: "add_picture.*figures/"
    - from: "slides/generate_deck.py"
      to: "output/presentation.pptx"
      via: "prs.save() call"
      pattern: "prs\\.save.*output.*presentation"
---

<objective>
Create the PowerPoint generation script that produces a complete 11-slide deck for the 30-minute conference presentation half. The script embeds pre-rendered figures from Phase 2, adds transition slides between demo segments, and attaches speaker notes to every slide.

Purpose: The slide deck provides narrative structure around the live notebook demo, giving the audience visual anchors and context during transitions between demo segments.
Output: `slides/generate_deck.py` (generation script), `output/presentation.pptx` (the deck)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-slide-deck/03-RESEARCH.md
@.planning/phases/02-demo-notebook-and-visualizations/02-01-SUMMARY.md

<interfaces>
<!-- From Phase 2 output: three PNG figure files generated by the demo notebook -->
<!-- These files exist in figures/ only AFTER running QVC_QNN_demo.ipynb -->
figures/input_data_grid.png   -- 2x4 pixel grid showing horizontal vs vertical line patterns
figures/loss_curve.png        -- Training convergence plot with batch boundary markers
figures/hardware_comparison.png -- Simulator vs hardware bar chart with value labels

<!-- python-pptx 1.0.2 API (verified in research) -->
from pptx import Presentation
from pptx.util import Inches, Pt
from pptx.enum.text import PP_ALIGN
from pptx.dml.color import RGBColor

# Default template layouts verified:
# Layout 0: Title Slide (title + subtitle placeholders)
# Layout 1: Title and Content (title + body with bullet support)
# Layout 5: Title Only (title placeholder, blank body area -- for figure slides)
# Layout 6: Blank (no placeholders -- for transition slides)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create slides/generate_deck.py and generate the presentation</name>
  <files>slides/generate_deck.py, output/presentation.pptx</files>
  <read_first>
    - .planning/phases/03-slide-deck/03-RESEARCH.md (verified API patterns, layouts, pitfalls)
    - .planning/phases/02-demo-notebook-and-visualizations/02-01-SUMMARY.md (figure filenames and content descriptions)
  </read_first>
  <action>
    Create `slides/` and `output/` directories. Create `slides/generate_deck.py` using python-pptx 1.0.2.

    The script must be runnable standalone: `python slides/generate_deck.py` from the project root.
    It must create `output/` with `os.makedirs("output", exist_ok=True)`.
    It must print status: "Adding slide: {title}..." for each slide and "Saved: output/presentation.pptx" at the end.

    **Helper function (define at top of script):**

    ```python
    def add_figure_or_placeholder(slide, figure_path, left, top, width, height):
        """Embed figure if it exists, otherwise add placeholder text box."""
        if os.path.exists(figure_path):
            slide.shapes.add_picture(figure_path, left, top, width, height)
        else:
            filename = os.path.basename(figure_path)
            txBox = slide.shapes.add_textbox(left, top, width, Inches(1))
            tf = txBox.text_frame
            tf.text = f"[Figure: {filename} -- run notebook first]"
            tf.paragraphs[0].font.size = Pt(18)
            tf.paragraphs[0].font.italic = True
            tf.paragraphs[0].alignment = PP_ALIGN.CENTER
    ```

    **Slide structure (11 slides total, in this exact order):**

    **Slide 1 — Title Slide (Layout 0, SLID-01)**
    - Title: "Quantum Variational Circuits for Classification"
    - Subtitle: "Training, Evaluation, and Hardware Execution"
    - Speaker notes: "This is the second half of our presentation. [Co-presenter] covered the theory and circuit design. I will now show you the classifier in action -- from training through real quantum hardware."

    **Slide 2 — Overview (Layout 1, SLID-01)**
    - Title: "What We'll Cover"
    - Body bullets: "The classification task -- 2x4 pixel grids", "Live training demo with a variational quantum circuit", "Evaluation results on the simulator", "Execution on real IBM quantum hardware"
    - Speaker notes: "Three sections, each with a live notebook demo followed by slides summarizing what we saw. I will switch between these slides and the Jupyter notebook throughout."

    **Slide 3 — Data Overview with Figure (Layout 5, SLID-03)**
    - Title: "The Classification Task"
    - Embed `figures/input_data_grid.png` at Inches(1), Inches(1.5), width=Inches(8), height=Inches(5.5) using `add_figure_or_placeholder`
    - Speaker notes: "We are classifying 2x4 pixel grids as horizontal or vertical line patterns. Simple enough for a quantum classifier, visual enough for the audience to follow. Each pixel maps to one qubit -- 8 qubits total."

    **Slide 4 — TRANSITION: Training (Layout 6, SLID-02)**
    - Add textbox at Inches(1), Inches(2.5), width=Inches(8), height=Inches(2)
    - Text: "Training the Quantum Classifier"
    - Font: Pt(36), bold=True, alignment=PP_ALIGN.CENTER, color=RGBColor(0x1A, 0x47, 0x80) (dark blue)
    - Speaker notes: "Now I will switch to the notebook and run the training live. You will see 3 iterations of the optimizer -- enough to see the loop working. Then we load the full pre-trained weights."

    **Slide 5 — Training Results with Figure (Layout 5, SLID-03)**
    - Title: "Training Convergence"
    - Embed `figures/loss_curve.png` at Inches(1), Inches(1.5), width=Inches(8), height=Inches(5.5) using `add_figure_or_placeholder`
    - Speaker notes: "The loss curve shows convergence over the full training run. The MSE loss drops steadily, confirming the variational circuit is learning the classification boundary. The live demo showed just 3 iterations for time, but the full training ran to convergence."

    **Slide 6 — TRANSITION: Evaluation (Layout 6, SLID-02)**
    - Add textbox at Inches(1), Inches(2.5), width=Inches(8), height=Inches(2)
    - Text: "Evaluating the Classifier"
    - Font: Pt(36), bold=True, alignment=PP_ALIGN.CENTER, color=RGBColor(0x1A, 0x47, 0x80)
    - Speaker notes: "Back to the notebook. We will run the trained model on our test set and see how well it generalizes."

    **Slide 7 — Evaluation Results (Layout 1, SLID-01)**
    - Title: "Classification Accuracy"
    - Body text: "Simulator accuracy on test set", add_paragraph: "The variational circuit correctly classifies horizontal vs vertical patterns", add_paragraph: "Trained with COBYLA optimizer on cached weights"
    - Speaker notes: "The classifier achieves strong accuracy on the simulator. This validates that the variational circuit learned the horizontal vs vertical pattern. Next: does it hold up on real quantum hardware?"

    **Slide 8 — TRANSITION: Hardware (Layout 6, SLID-02)**
    - Add textbox at Inches(1), Inches(2.5), width=Inches(8), height=Inches(2)
    - Text: "Running on Real Quantum Hardware"
    - Font: Pt(36), bold=True, alignment=PP_ALIGN.CENTER, color=RGBColor(0x1A, 0x47, 0x80)
    - Speaker notes: "The real test -- running our trained classifier on an actual IBM quantum processor. Noise, decoherence, and gate errors all come into play."

    **Slide 9 — Hardware Results with Figure (Layout 5, SLID-03)**
    - Title: "Simulator vs Hardware"
    - Embed `figures/hardware_comparison.png` at Inches(1), Inches(1.5), width=Inches(8), height=Inches(5.5) using `add_figure_or_placeholder`
    - Speaker notes: "Side-by-side comparison. The hardware results show the impact of real-world quantum noise. Despite this, the classifier still performs -- demonstrating that variational circuits have noise resilience. The gap between simulator and hardware is the current frontier."

    **Slide 10 — Summary/Takeaway (Layout 1, SLID-04)**
    - Title: "Key Takeaways"
    - Body bullets (each via add_paragraph after setting tf.text for the first):
      1. "Variational quantum circuits can learn classification tasks"
      2. "Training converges reliably with classical optimizers"
      3. "Real hardware execution is feasible today -- with caveats"
      4. "Cache-first strategy makes live quantum demos practical"
    - Speaker notes: "Four things to remember. VQCs work. Training is tractable. Hardware is noisy but functional. And with the right preparation, you can run quantum ML live at a conference."

    **Slide 11 — Q&A (Layout 1, SLID-01)**
    - Title: "Questions?"
    - Body text: "Thank you"
    - Speaker notes: "Open for questions. If asked about noise mitigation, mention error suppression techniques. If asked about scaling, mention current qubit limits and the path forward with error correction."

    After building all slides, call `prs.save("output/presentation.pptx")`.

    Run the script after creating it: `python slides/generate_deck.py`
  </action>
  <verify>
    <automated>cd /Users/manta/Downloads/Quantum_Variational_Circuits && python slides/generate_deck.py && python -c "
from pptx import Presentation
p = Presentation('output/presentation.pptx')
n = len(p.slides)
print(f'Slides: {n}')
assert n >= 10, f'Expected >=10, got {n}'
# Check every slide has notes
for i, s in enumerate(p.slides):
    notes = s.notes_slide.notes_text_frame.text.strip()
    assert len(notes) > 0, f'Slide {i+1} missing notes'
print('All slides have speaker notes')
# Check for figure references (images or placeholders)
img_count = sum(1 for s in p.slides for sh in s.shapes if sh.shape_type == 13)
texts = [sh.text for s in p.slides for sh in s.shapes if sh.has_text_frame]
placeholder_count = sum(1 for t in texts if '[figure:' in t.lower())
print(f'Images: {img_count}, Placeholders: {placeholder_count}')
assert img_count + placeholder_count >= 3, 'Need >= 3 figure refs'
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - slides/generate_deck.py exists and runs without error via `python slides/generate_deck.py`
    - output/presentation.pptx is generated with exactly 11 slides
    - grep -c "add_picture\|add_figure_or_placeholder" slides/generate_deck.py returns >= 3 (three figure embeds)
    - grep -c "notes_slide" slides/generate_deck.py returns >= 11 (every slide gets notes)
    - grep -c "Layout 6\|slide_layouts\[6\]" slides/generate_deck.py returns >= 3 (three transition slides)
    - grep "os.path.exists" slides/generate_deck.py confirms graceful figure handling
    - Script prints "Saved: output/presentation.pptx" on success
  </acceptance_criteria>
  <done>Running `python slides/generate_deck.py` produces `output/presentation.pptx` with 11 slides: title, overview, 3 figure slides (with placeholder fallback), 3 transition slides, evaluation text slide, summary slide, and Q&A slide -- all with speaker notes</done>
</task>

<task type="auto">
  <name>Task 2: Create test suite validating slide deck against SLID requirements</name>
  <files>tests/test_slide_deck.py</files>
  <read_first>
    - slides/generate_deck.py (understand the slide structure, titles, and layout choices)
    - .planning/REQUIREMENTS.md (SLID-01 through SLID-04 definitions)
  </read_first>
  <action>
    Create `tests/test_slide_deck.py` with pytest tests validating the generated deck against all four SLID requirements. The test file must:

    1. Use a module-scoped fixture that runs `subprocess.run(["python", "slides/generate_deck.py"], check=True)` and loads the resulting pptx
    2. Define a helper `_all_slide_texts(prs)` that extracts all text from all shapes across all slides

    **Required test functions (8 tests):**

    ```python
    import pytest
    import subprocess
    import os
    from pptx import Presentation

    PPTX_PATH = "output/presentation.pptx"

    @pytest.fixture(scope="module")
    def generated_deck():
        """Run generate_deck.py and load the resulting .pptx."""
        subprocess.run(["python", "slides/generate_deck.py"], check=True)
        assert os.path.exists(PPTX_PATH), f"{PPTX_PATH} not generated"
        return Presentation(PPTX_PATH)

    def _all_slide_texts(prs):
        """Extract all text from all shapes in all slides."""
        texts = []
        for slide in prs.slides:
            for shape in slide.shapes:
                if shape.has_text_frame:
                    texts.append(shape.text)
        return texts
    ```

    **SLID-01 tests (deck covers training, evaluation, hardware):**

    `test_slide_count_minimum(generated_deck)` -- assert len >= 10

    `test_training_section_exists(generated_deck)` -- assert any text contains "training" (case-insensitive)

    `test_evaluation_section_exists(generated_deck)` -- assert any text contains "evaluat" or "accuracy" (case-insensitive)

    `test_hardware_section_exists(generated_deck)` -- assert any text contains "hardware" (case-insensitive)

    **SLID-02 test (transition slides):**

    `test_transition_slides_exist(generated_deck)` -- Count slides where text contains "Training the Quantum", "Evaluating the", or "Running on Real". Assert count >= 3.

    **SLID-03 test (figures embedded):**

    `test_figure_references(generated_deck)` -- Count shapes with shape_type == 13 (MSO_SHAPE_TYPE.PICTURE) across all slides. Also count text containing "[figure:" (placeholder fallback). Assert images + placeholders >= 3.

    **SLID-04 test (summary slide):**

    `test_summary_slide_exists(generated_deck)` -- assert any text contains "key takeaway" (case-insensitive)

    **Speaker notes test (all slides):**

    `test_all_slides_have_speaker_notes(generated_deck)` -- For each slide, assert `slide.notes_slide.notes_text_frame.text.strip()` is non-empty. Include slide index in error message.
  </action>
  <verify>
    <automated>cd /Users/manta/Downloads/Quantum_Variational_Circuits && python -m pytest tests/test_slide_deck.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_slide_deck.py exists with exactly 8 test functions
    - grep -c "def test_" tests/test_slide_deck.py returns 8
    - grep "SLID-01\|SLID-02\|SLID-03\|SLID-04" tests/test_slide_deck.py confirms all requirement IDs referenced in comments
    - All 8 tests pass when run with `python -m pytest tests/test_slide_deck.py -v`
    - Test output shows 8 passed, 0 failed
  </acceptance_criteria>
  <done>All 8 pytest tests pass, validating SLID-01 (training/eval/hardware sections + slide count), SLID-02 (3 transition slides), SLID-03 (3 figure embeds or placeholders), SLID-04 (summary slide), and speaker notes on every slide</done>
</task>

</tasks>

<verification>
1. `python slides/generate_deck.py` runs without errors and prints "Saved: output/presentation.pptx"
2. `python -m pytest tests/test_slide_deck.py -v` -- all 8 tests pass
3. `python -m pytest tests/ -v` -- full test suite remains green (no regressions)
</verification>

<success_criteria>
- output/presentation.pptx exists with 11 slides covering training, evaluation, and hardware sections
- 3 transition slides provide context between demo segments with 36pt bold centered text
- 3 figure slides embed notebook-generated PNGs (or show clear placeholders if figures/ not yet populated)
- Summary slide presents 4 key takeaways as bullet points
- Every slide has presenter-ready speaker notes
- All 8 automated tests pass validating SLID-01 through SLID-04
</success_criteria>

<output>
After completion, create `.planning/phases/03-slide-deck/03-01-SUMMARY.md`
</output>
