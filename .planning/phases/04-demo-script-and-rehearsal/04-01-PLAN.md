---
phase: 04-demo-script-and-rehearsal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo_script.md
  - tests/test_demo_script.py
autonomous: true
requirements: [SCRP-01, SCRP-02, SCRP-03, SCRP-04]

must_haves:
  truths:
    - "A timing table accounts for every minute of the ~30-minute half with section durations summing to 28-32 minutes"
    - "Speaker notes exist for all 11 slides and all 3 demo segments (training, evaluation, hardware) with what-to-say and what-to-show guidance"
    - "A dedicated hardware fallback section contains a decision tree covering live results available, cached results fallback, and emergency verbal fallback"
    - "Co-presenter handoff cues specify incoming handoff (start of half) and outgoing handoff (end of half) with verbal phrases"
    - "A pre-talk checklist covers JupyterLab setup, cache file verification, and projector testing"
  artifacts:
    - path: "demo_script.md"
      provides: "Complete presenter script for 30-minute conference half"
      contains: "Timing Overview"
    - path: "tests/test_demo_script.py"
      provides: "Automated validation of SCRP-01 through SCRP-04"
      exports: ["test_timing_breakdown", "test_speaker_notes_coverage", "test_hardware_fallback", "test_copresenter_handoff"]
  key_links:
    - from: "demo_script.md"
      to: "slides/generate_deck.py"
      via: "References all 11 slide numbers and titles"
      pattern: "Slide [0-9]+"
    - from: "demo_script.md"
      to: "QVC_QNN_demo.ipynb"
      via: "References notebook cells by number for each demo segment"
      pattern: "Cell [0-9]+"
    - from: "tests/test_demo_script.py"
      to: "demo_script.md"
      via: "Reads and parses markdown content"
      pattern: "open.*demo_script"
---

<objective>
Create the complete presenter script (demo_script.md) for the 30-minute conference half, covering timing marks, enriched speaker notes, hardware fallback decision tree, and co-presenter handoff protocol. Also create the test suite validating all SCRP requirements.

Purpose: The presenter needs a single rehearsal document that ties together the 14-cell demo notebook, 11-slide PowerPoint deck, and cache infrastructure into a deliverable presentation with fallback plans.
Output: demo_script.md (project root), tests/test_demo_script.py
</objective>

<execution_context>
@/Users/manta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/manta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-script-and-rehearsal/04-RESEARCH.md
@.planning/phases/03-slide-deck/03-01-SUMMARY.md
@.planning/phases/02-demo-notebook-and-visualizations/02-01-SUMMARY.md

<interfaces>
<!-- Key references the executor needs from existing artifacts -->

From slides/generate_deck.py — all 11 slide titles and notes:
- Slide 1: "Quantum Variational Circuits for Classification" (Title)
- Slide 2: "What We'll Cover" (Overview)
- Slide 3: "The Classification Task" (Data Overview + input_data_grid.png)
- Slide 4: "Training the Quantum Classifier" (Transition)
- Slide 5: "Training Convergence" (loss_curve.png)
- Slide 6: "Evaluating the Classifier" (Transition)
- Slide 7: "Classification Accuracy" (Evaluation results)
- Slide 8: "Running on Real Quantum Hardware" (Transition)
- Slide 9: "Simulator vs Hardware" (hardware_comparison.png)
- Slide 10: "Key Takeaways" (Summary, SLID-04)
- Slide 11: "Questions?" (Q&A)

From QVC_QNN_demo.ipynb — 14 cells in presentation order:
- Cell 0: Title markdown
- Cell 1: Setup (imports, rcParams)
- Cell 2: Cache file guard
- Cell 3: Data overview markdown
- Cell 4: Data generation + input grid visualization (VIZ-04)
- Cell 5: Circuit explanation markdown
- Cell 6: Live training demo (3 COBYLA iterations, D-07)
- Cell 7: Load cached weights
- Cell 8: Training results markdown
- Cell 9: Loss curve plot (VIZ-02)
- Cell 10: Evaluation (forward pass + accuracy)
- Cell 11: Hardware results markdown
- Cell 12: Hardware comparison plot (VIZ-03, auto-detects live vs cached)
- Cell 13: Key findings markdown

Cache files (project root):
- trained_weights.json
- training_history.json
- hardware_results.json
- live_results.json (optional, auto-detected by Cell 12)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test suite for demo script validation</name>
  <files>tests/test_demo_script.py</files>
  <read_first>
    - .planning/phases/04-demo-script-and-rehearsal/04-RESEARCH.md (test strategy and Wave 0 gaps section)
    - tests/test_slide_deck.py (existing test patterns in project)
  </read_first>
  <action>
Create tests/test_demo_script.py with pytest tests that validate demo_script.md content by reading and parsing the markdown file. Use the same test style as the existing tests/test_slide_deck.py.

The test file must contain these test functions:

1. **test_timing_breakdown** (SCRP-01): Read demo_script.md, verify it contains a "Timing Overview" section with a markdown table. Parse the table rows and verify: (a) at least 6 rows of timed sections, (b) each row has a time range in [MM:SS-MM:SS] or [MM:SS] format, (c) the durations sum to between 28 and 32 minutes inclusive. Use regex to extract time values.

2. **test_speaker_notes_coverage** (SCRP-02): Verify all 11 slides are referenced by checking for strings "Slide 1" through "Slide 11" (case-insensitive). Also verify all 3 demo segments are covered by checking for section headers containing "Training", "Evaluation", and "Hardware" (case-insensitive). Each slide reference must appear in a section that also contains "Say:" or "say:" or "Speaker" content.

3. **test_hardware_fallback** (SCRP-03): Verify a dedicated section exists with heading containing "Fallback" (case-insensitive). Within that section, verify three scenarios are covered by checking for these strings: "live_results.json", "hardware_results.json", and at least one of "simulator" or "verbally" (the emergency fallback). Also verify the section does NOT contain apologetic language: assert "unfortunately" not in fallback_section.lower() and "sorry" not in fallback_section.lower().

4. **test_copresenter_handoff** (SCRP-04): Verify a dedicated section exists with heading containing "Handoff" (case-insensitive). Within that section, verify: (a) "incoming" or "start" appears (incoming handoff), (b) "outgoing" or "end" appears (outgoing handoff), (c) at least one quoted verbal phrase exists (text between quotation marks).

5. **test_pretalk_checklist**: Verify a "Checklist" section exists containing at least 8 checkbox items (lines matching `- [ ]`). Verify these keywords appear in checklist items: "JupyterLab", "cache", "projector" or "screen".

Helper: Write a helper function `_read_script()` that reads demo_script.md from the project root (use `os.path.join(os.path.dirname(os.path.dirname(__file__)), "demo_script.md")`). Another helper `_get_section(content, heading)` that extracts text between a heading matching the given string and the next heading of equal or higher level.
  </action>
  <verify>
    <automated>cd /Users/manta/Downloads/Quantum_Variational_Circuits && python -m pytest tests/test_demo_script.py --collect-only 2>&1 | grep "test session starts\|<Function\|error"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_demo_script.py exists and contains 5 test functions
    - `grep -c "def test_" tests/test_demo_script.py` returns 5
    - `grep "SCRP-01\|SCRP-02\|SCRP-03\|SCRP-04" tests/test_demo_script.py` shows all 4 requirement IDs referenced in comments or docstrings
    - `python -m pytest tests/test_demo_script.py --collect-only` collects 5 tests without import errors
  </acceptance_criteria>
  <done>Five pytest tests collected successfully, each mapping to a SCRP requirement (plus checklist). Tests will fail until Task 2 creates demo_script.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create demo_script.md with timing, speaker notes, fallback plan, and handoff cues</name>
  <files>demo_script.md</files>
  <read_first>
    - .planning/phases/04-demo-script-and-rehearsal/04-RESEARCH.md (timing architecture, fallback decision tree, handoff protocol, pre-talk checklist, existing content inventory)
    - slides/generate_deck.py (all 11 slide titles and existing speaker notes)
    - QVC_QNN_demo.ipynb (14-cell structure and content)
  </read_first>
  <action>
Create demo_script.md in the project root following the single-document presenter script pattern from the research. The file must contain these sections in order:

**1. Title and Purpose**
```
# QVC Conference Presentation - Demo Script
```
Brief intro: "Single reference document for delivering the 30-minute training, evaluation, and hardware execution half."

**2. Pre-Talk Checklist**
Section heading: `## Pre-Talk Checklist`
At least 10 checkbox items covering:
- [ ] JupyterLab open with QVC_QNN_demo.ipynb
- [ ] JupyterLab font size increased (Cmd+= several times until code is readable from back row)
- [ ] JupyterLab sidebar collapsed (View > Toggle Left Sidebar)
- [ ] Kernel restarted, no cells pre-executed
- [ ] Cache files verified: trained_weights.json, training_history.json, hardware_results.json all present in project root
- [ ] PowerPoint output/presentation.pptx open in presenter mode on secondary display
- [ ] .env file with IBM_QUANTUM_API_KEY present on demo machine
- [ ] WiFi confirmed working (or mobile hotspot ready as backup)
- [ ] Screen sharing / projector tested with both JupyterLab and PowerPoint
- [ ] Optional: during co-presenter's half, submit live hardware job via QVC_QNN.ipynb and note job ID

**3. Timing Overview**
Section heading: `## Timing Overview`
Markdown table with columns: Section, Time, Duration, Slides, Notebook Cells.
Use the exact timing from research:
| Section | Time | Duration | Slides | Notebook Cells |
|---------|------|----------|--------|----------------|
| Handoff and Introduction | 00:00-02:00 | 2 min | 1-2 | -- |
| Data Overview | 02:00-05:00 | 3 min | 3 | 0-4 |
| Training Demo | 05:00-15:00 | 10 min | 4-5 | 5-9 |
| Evaluation | 15:00-20:00 | 5 min | 6-7 | 10 |
| Hardware Execution | 20:00-27:00 | 7 min | 8-9 | 11-12 |
| Takeaways and Q&A | 27:00-30:00 | 3 min | 10-11 | 13 |

Add a note: "Total: ~30 minutes. Buffer of ~2 minutes distributed across sections. If running long after Training Demo, shorten Evaluation explanations."

**4. Section-by-Section Script**
For EACH of the 6 sections above, create a subsection with this format:
```
## [MM:SS-MM:SS] Section Name (N min)
```

Each section must include:
- **Slides:** which slide numbers
- **Notebook Cells:** which cells (or "--" if none)
- **Actions:** step-by-step what to click, run, switch to
- **Say:** actual talking points (enriched beyond generate_deck.py notes). These are the SCRP-02 speaker notes.
- **Timing Note:** what to do if running long/short

Specific content per section (enriched from research + generate_deck.py notes):

**[00:00-02:00] Handoff and Introduction (2 min)**
- Slides: 1-2
- Actions: Take podium from co-presenter, advance to Slide 1, then Slide 2
- Say: "Thanks [co-presenter]. You have seen the theory and circuit design -- now let us train this classifier and run it on real quantum hardware." Then on Slide 2: "We will cover three things: the classification task, live training, and execution on real IBM hardware. I will switch between these slides and a live Jupyter notebook."

**[02:00-05:00] Data Overview (3 min)**
- Slides: 3, Notebook Cells: 0-4
- Actions: Show Slide 3 (input grid figure), then switch to JupyterLab, run Cells 0-4 (setup, cache guard, data generation, visualization)
- Say: "Our task is classifying 2x4 pixel grids -- horizontal versus vertical line patterns. Each pixel maps to one qubit, so 8 qubits total. Let me show you the data in the notebook." After switching: "Cell 0-1 set up our environment. Cell 2 checks our cache files are ready. And here is our dataset -- you can see the horizontal and vertical patterns clearly."
- Timing: This section is straightforward. If audience asks questions about the data, keep answers brief.

**[05:00-15:00] Training Demo (10 min) -- HEAVIEST SECTION**
- Slides: 4-5, Notebook Cells: 5-9
- Actions: Show Slide 4 (transition), switch to notebook. Run Cell 6 (live 3-iteration COBYLA training -- expect 10-30 seconds). While waiting, explain what is happening. Run Cell 7 (load cached full weights -- instant). Run Cell 9 (loss curve -- instant). Switch back to Slide 5 (training convergence figure).
- Say: On Slide 4: "Now let us train the classifier live." Switch to notebook: "Cell 5 explains the circuit architecture -- a feature map encodes our pixel data, and a variational ansatz learns the classification boundary." Run Cell 6: "Watch the optimizer output -- each iteration adjusts the circuit parameters to minimize our loss function. We are running just 3 iterations for time, but the full training ran to convergence." After Cell 6 completes: "Now let me load the fully trained weights." Run Cell 7: "These weights come from a complete training run we cached beforehand." Run Cell 9: "And here is the full loss curve showing convergence -- the loss drops steadily across all epochs and batches."
- Timing: Cell 6 is the most variable (10-30 seconds). If it takes >20 seconds, skip detailed explanation of each iteration. If running 2+ minutes over at end of this section, abbreviate the loss curve discussion.

**[15:00-20:00] Evaluation (5 min)**
- Slides: 6-7, Notebook Cells: 10
- Actions: Show Slide 6 (transition), switch to notebook. Run Cell 10 (forward pass on test set + accuracy). Switch to Slide 7 (classification accuracy).
- Say: On Slide 6: "Let us see how well our trained classifier generalizes." Switch to notebook: "Cell 10 runs the trained model on our held-out test set using the simulator." After Cell 10: "You can see the accuracy printed -- the classifier correctly distinguishes horizontal from vertical patterns." Switch to Slide 7: "Strong accuracy on the simulator validates that the variational circuit learned the pattern. But the real question is: does it hold up on actual quantum hardware?"
- Timing: If running long, skip switching to the notebook and just show Slide 7 with verbal reference to the accuracy number.

**[20:00-27:00] Hardware Execution (7 min)**
- Slides: 8-9, Notebook Cells: 11-12
- Actions: Show Slide 8 (transition), switch to notebook. Run Cell 12 (hardware comparison -- auto-detects live_results.json or falls back to hardware_results.json). Switch to Slide 9 (hardware comparison figure).
- Say: On Slide 8: "Now for the real test -- running on an actual IBM quantum processor." Switch to notebook: Check the hardware fallback decision tree (see dedicated section below) to determine what to say based on whether live results are available. After Cell 12 produces the comparison chart: "Here is the side-by-side comparison. The hardware results show the impact of real-world quantum noise -- gate errors, decoherence, crosstalk. But notice the classifier still performs. Variational circuits have inherent noise resilience because the classical optimizer can partially compensate for hardware imperfections."
- Timing: This section has the most audience interest. Allow time for the comparison discussion. If running short on time, reduce the noise explanation.

**[27:00-30:00] Takeaways and Q&A (3 min)**
- Slides: 10-11, Notebook Cells: 13
- Actions: Switch to Slide 10 (Key Takeaways). Pause between each point. Advance to Slide 11 (Q&A).
- Say: "Four things to take away. First: variational quantum circuits can learn classification tasks. Second: training converges reliably with classical optimizers like COBYLA. Third: real hardware execution is feasible today -- with caveats around noise. And fourth: a cache-first strategy makes live quantum demos practical. Thank you -- I am happy to take questions." Then handle Q&A or hand off to co-presenter.
- Anticipated questions: (a) "How does noise mitigation work?" -- mention error suppression techniques and dynamical decoupling. (b) "How does this scale to larger problems?" -- mention current qubit limits (100+) and the path forward with error correction. (c) "Why COBYLA and not gradient-based?" -- parameter shift rule is expensive on hardware; COBYLA is gradient-free and works well for small circuits. (d) "Could you use a classical neural network instead?" -- yes, and it would likely outperform for this simple task; the point is demonstrating the quantum approach works.

**5. Hardware Fallback Decision Tree**
Section heading: `## Hardware Fallback Decision Tree`
Content from research verbatim (the decision tree with three branches):

Pre-talk: Submit live hardware job during co-presenter's half. Note job ID.

When reaching Hardware section (Slide 8-9, ~22 min in):

**If live_results.json exists:**
- Say: "We submitted this job during [co-presenter]'s half, and it completed."
- Action: Cell 12 auto-detects live_results.json and shows the LIVE-annotated figure.

**If only hardware_results.json exists (cached, always available):**
- Say: "We pre-ran this on IBM Quantum hardware to ensure we have results to show you."
- Action: Cell 12 falls back to hardware_results.json automatically.
- IMPORTANT: Do NOT say "unfortunately" or "sorry" -- present the cache confidently as preparation, not failure.

**If neither exists (should never happen with Phase 1 cache):**
- Say: "Hardware results are being processed. Let me show you what we expect based on the simulator, and discuss how hardware noise typically affects these results."
- Action: Show simulator results from Cell 10 and explain expected hardware degradation verbally.

**6. Co-Presenter Handoff Protocol**
Section heading: `## Co-Presenter Handoff Protocol`

**Incoming Handoff (start of your half):**
- Co-presenter finishes circuit design section
- Co-presenter says: "[Your name] will now show us this circuit in action"
- You take the podium, advance to Slide 1
- Opening line: "Thanks [co-presenter]. You have seen the theory -- now let us train this classifier and run it on real hardware."

**Outgoing Handoff (end of your half):**
- After Slide 11 (Q&A), if co-presenter handles closing:
- Verbal cue: "I will hand it back to [co-presenter] for any final thoughts."
- If handling Q&A jointly, no outgoing handoff needed -- stay at podium together.

**7. Anticipated Questions**
Section heading: `## Anticipated Questions`
List 4-6 questions with prepared answers (the ones from the Q&A section above, plus 1-2 more).
  </action>
  <verify>
    <automated>cd /Users/manta/Downloads/Quantum_Variational_Circuits && python -m pytest tests/test_demo_script.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - demo_script.md exists in project root
    - `grep -c "## " demo_script.md` returns at least 10 (section headers)
    - `grep "Slide 1\|Slide 2\|Slide 3\|Slide 4\|Slide 5\|Slide 6\|Slide 7\|Slide 8\|Slide 9\|Slide 10\|Slide 11" demo_script.md | wc -l` shows all 11 slides referenced
    - `grep -c "\- \[ \]" demo_script.md` returns at least 8 (checklist items)
    - `grep -i "fallback" demo_script.md` finds the hardware fallback section
    - `grep -i "handoff" demo_script.md` finds the co-presenter handoff section
    - `grep "live_results.json" demo_script.md` finds the live results branch
    - `grep "hardware_results.json" demo_script.md` finds the cached results branch
    - All 5 tests in tests/test_demo_script.py pass
  </acceptance_criteria>
  <done>demo_script.md is a complete presenter reference document. All 5 pytest tests pass, confirming SCRP-01 (timing breakdown sums to ~30 min), SCRP-02 (all 11 slides and 3 demo segments covered), SCRP-03 (hardware fallback decision tree with 3 branches), and SCRP-04 (incoming and outgoing handoff cues with verbal phrases).</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_demo_script.py -v` -- all 5 tests pass
2. `python -m pytest tests/ -v` -- full suite passes (existing 16 tests + 5 new = 21 total, with expected skips for cache tests)
3. `wc -l demo_script.md` -- substantial document (expect 200+ lines)
4. Manual spot check: timing table durations sum to ~30 minutes
</verification>

<success_criteria>
- demo_script.md exists in project root as a single comprehensive presenter document
- All 4 SCRP requirements validated by passing automated tests
- Timing breakdown covers all 6 sections summing to ~30 minutes
- Speaker notes reference all 11 slides and all 14 notebook cells across 3 demo segments
- Hardware fallback decision tree has 3 branches (live, cached, emergency) with no apologetic language
- Co-presenter handoff protocol covers incoming and outgoing transitions with verbal cues
- Pre-talk checklist has 10+ actionable items
- Full test suite (tests/) passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-script-and-rehearsal/04-01-SUMMARY.md`
</output>
